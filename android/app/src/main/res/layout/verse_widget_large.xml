<?xml version="1.0" encoding="utf-8"?>
<!--
  Large (4×2) widget layout. Mirrors the "Large (4 × 2)" section of the
  design's Widget screen: gold chip + "JOHN 3:16" eyebrow, verse excerpt in
  italic serif, footer row (reference · translation ←→ streak).
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/verse_widget_bg_large"
    android:padding="18dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <View
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:background="@drawable/widget_chip_dot" />

        <TextView
            android:id="@+id/widget_eyebrow"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:fontFamily="@font/inter_regular"
            android:textStyle="bold"
            android:textSize="11sp"
            android:letterSpacing="0.12"
            android:textColor="@color/widget_accent"
            android:text="@string/widget_eyebrow_text" />
    </LinearLayout>

    <TextView
        android:id="@+id/widget_verse_large"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="10dp"
        android:gravity="top"
        android:fontFamily="@font/lora_italic"
        android:textStyle="italic"
        android:textSize="17sp"
        android:lineSpacingMultiplier="1.15"
        android:maxLines="3"
        android:ellipsize="end"
        android:textColor="@color/widget_text_primary"
        tools:text="For God so loved the world, that he gave his only begotten Son…" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/widget_footer_left"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:fontFamily="@font/inter_regular"
            android:textSize="11.5sp"
            android:textColor="@color/widget_text_secondary"
            android:maxLines="1"
            android:ellipsize="end"
            tools:text="John 3:16 · KJV" />

        <TextView
            android:id="@+id/widget_streak_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/inter_regular"
            android:textStyle="bold"
            android:textSize="11.5sp"
            android:textColor="@color/widget_accent"
            tools:text="12-day streak" />
    </LinearLayout>
</LinearLayout>
